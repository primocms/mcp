{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "block-fields.schema.json",
  "title": "Primo block fields.yaml",
  "description": "Schema for blocks/{name}/fields.yaml. The file is a bare top-level list of field definitions — no wrapper object.",
  "type": "array",
  "items": {
    "$ref": "field.schema.json#/$defs/field"
  },
  "default": []
}
